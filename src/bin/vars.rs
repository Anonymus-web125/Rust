fn main(){
    let x: i32 = 5;
    let y: i32 = 7;
    // There are many types of integer data types in rust such as i8,i16,i32,i64 etc where 8,16,32,64 represents the number of bits
    if x == y{
        println!("Equals");
    }else{
        println!("Not equals");
    }
}